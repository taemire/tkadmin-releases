# 트러블슈팅 및 로그 진단

장애 상황에서 `tkctl logs`, `tkctl status` 등의 통합 명령어를 활용해 문제를 신속하게 식별하고 조치하는 가이드입니다.

- **로그 스냅샷 추출**: `tkctl logs export --since "1h"` -> `/tmp/tums_logs.tar.gz`
- 주요 오류 분석표
- 권한 충돌 해제 방법
